<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Uzz Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(-135deg, #ffa1e3, #3d3d3d);
        }
        .btn {
            font-size: 1.1rem;
            padding: 0.5rem 1rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .btn:hover {
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.5);
        }
        .btn-success:hover {
            background-color: #28a745;
            border-color: #28a745;
        }
        .btn-danger:hover {
            background-color: #dc3545;
            border-color: #dc3545;
        }
        .btn-primary:hover {
            background-color: #007bff;
            border-color: #007bff;
        }
        .search-container {
            display: flex;
            justify-content: center;
        }
        .search-container .input-group {
            max-width: 500px;
            width: 100%;
        }
        .table {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
            background-color: #3a3232;
        }
        .table th, .table td {
            vertical-align: middle;
            text-align: center;
            background-color: #464242;
            color: white;
            letter-spacing: 1px;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h1 class="text-center" style="font-weight: bold;">
            User Management 📝
        </h1>
        <div class="row justify-content-between mt-3">
            <div class="col">
                <a href="/admin/adduser" class="btn btn-success">Add New User</a>
            </div>
            <div class="col-auto">
                <a href="/admin/adminlogout" class="btn btn-danger">Logout</a>
            </div>
        </div>
        <div class="search-container mt-3">
            <form action="/admin/adhome" method="post" class="w-100">
                <div class="input-group">
                    <input type="text" name="search" id="search" class="form-control" placeholder="Enter the name of User to be Searched">
                    <div class="input-group-append">
                        <button class="btn btn-success" type="submit">Search</button>
                    </div>
                </div>
            </form>
        </div>
        <div class="text-center mt-3">
            <a href="/admin/adhome" class="btn btn-success">View All</a>
        </div>
        <table class="table table-bordered mt-4">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>E-Mail</th>
                    <th>Password</th>
                    <th>Update</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                {{#each users}}
                <tr>
                    <td>{{username}}</td>
                    <td>{{email}}</td>
                    <td>*********</td>
                    <td>
                        <a href="/admin/update/{{email}}" class="btn btn-primary">Update</a>
                    </td>
                    <td>
                        <a href="/admin/delete/{{email}}" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
